<div class="page-header">
  <h1><%=t '.title', :default => "Browse by Benefit or Classification" %></h1>
</div>

<% @tags.each do |tag| %>

<%= link_to( "<button class='btn btn-info'> (#{tag.items.count} products) #{tag.title} </button>".html_safe, tag, style: 'color:#FFFFFF; margin:2px;') %>


<% end %>

